{{> menu }}

<div class="container">
    <div class="row text-center p-4"><h2>Gameweek {{gameweek}}</h2></div>
    <table class="table table-dark" id="fixture-list">
        <thead>
            <tbody>
            {{#each fixtureList}}
            <tr>
                <td>{{homeTeam}}</td>
                <td>{{result}}</td>
                <td>{{awayTeam}}</td>
            </tr>
            {{/each}}
            </tbody>
        </thead>
    </table>
</div>