<header>
    <div class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom">
        <div class="container d-flex justify-content-between">
            <a class="navbar-brand" href="#">Football Hub</a>
            <div>
                <ul class="nav navbar-nav">
                    {{#each teamCrests}}
                    <li class="nav-item">
                        <a class="nav-link active" href="#"><img src="{{this}}" width="35" height="35"></a>
                    </li>
                    {{/each}}
            </div>
        </div>
    </div>
</header>